class Solution(object):
    def containsDuplicate(self, nums):
        duplicateSet = set()

        for num in nums:
            if num not in duplicateSet:
                duplicateSet.add(num)
            else:
                return True
        return False
        