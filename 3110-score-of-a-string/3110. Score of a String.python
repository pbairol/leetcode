class Solution(object):
    def scoreOfString(self, s):
        score = 0
        for i in range(1,len(s)):
            diff = abs(ord(s[i])-ord(s[i-1]))
            score += diff
        

        return score
        """
        :type s: str
        :rtype: int
        """
        