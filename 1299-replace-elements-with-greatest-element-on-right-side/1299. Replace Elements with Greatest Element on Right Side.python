class Solution(object):
    def replaceElements(self, arr):
        res = []
        maxLocal = -1

        for i in range(len(arr)-1,-1,-1):
            res.insert(0,maxLocal)
            if maxLocal < arr[i]:
                maxLocal = arr[i]
        return res
        